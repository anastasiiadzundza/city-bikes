export interface Company {
    id: string,
    name: string,
    city: string
}

export interface Station {
    id: string,
    name: string,
    emptySlots: string,
    freeBikes: string,
    latitude: string,
    longitude: string
}